{
  "agent": {
    "id": "inline_web_agent",
    "name": "inline_web_agent",
    "model": "gpt-4o",
    "temperature": 0.2,
    "max_turns": 20,
    "timeout": 600,
    "system_prompt": "You are a concise web assistant.",
    "skill_instructions": [
      {
        "id": "inline-web-skill:v1",
        "name": "inline-web-skill",
        "description": "Use web tools when web content is needed.",
        "instruction": "Use the provided tools when web content is needed."
      }
    ],
    "tools": [
      {
        "name": "web_fetch",
        "type": "http",
        "description": "Fetch a webpage and return the response body and metadata.",
        "endpoint": "http://web-tool-mcp.us-west1.infra.seaart.dev/tools/web_fetch",
        "method": "POST",
        "response_mode": "json",
        "poll_interval": 5.0,
        "poll_timeout": 600.0,
        "input_schema": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string"
            },
            "no_ssl": {
              "type": "boolean"
            },
            "include_headers": {
              "type": "boolean"
            }
          },
          "required": ["url"]
        }
      }
    ],
    "config": {}
  }
}
